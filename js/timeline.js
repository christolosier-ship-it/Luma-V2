const TimelineScreen = {
  selectedDate: todayStr(),
  selectedProtocolId: 'all',
  hideEmptyDays: false,
  _hideEmptyStorageKey: "luma.timeline.hideEmptyDays",
  async render() {
    const screen = document.getElementById('screen-timeline');
    if (localStorage.getItem(this._hideEmptyStorageKey) != null) this.hideEmptyDays = localStorage.getItem(this._hideEmptyStorageKey) === 'true';
    const [medications, phases, allActions, protocolEvents, protocols, notes, symptomsEntries, dosageOverrides] = await Promise.all([DB.getMedications(), DB.getPhases(), DB.getAllIntakeActions(), DB.getProtocolEvents(), DB.getProtocols(), DB.getDailyNotes(), DB.getDailySymptoms(), DB.getDosageOverrides()]);
    const actionsMap = Intakes.buildActionsMap(allActions);
    const days = []; for (let i=-4;i<=21;i++){ const d=fromDateStr(todayStr()); d.setDate(d.getDate()+i); days.push(toDateStr(d)); }
    const htmlDays = days.map(ds=>this._dayHtml(ds, medications, phases, actionsMap, protocolEvents, protocols, notes, symptomsEntries, dosageOverrides)).filter(Boolean).join('');
    const protocolOpts = ['<option value="all">Tous protocoles</option>'].concat(protocols.map(p=>`<option value="${escHtml(p.id)}" ${this.selectedProtocolId===p.id?'selected':''}>${escHtml(p.name)} (${escHtml(protocolStatusLabelFR(p.status))})</option>`)).join('');
    screen.innerHTML = `<div class="timeline-shell"><div class="timeline-toolbar"><select id="timeline-protocol-filter" class="form-input">${protocolOpts}</select><button id="timeline-jump-today" class="btn-settings btn-timeline-today">Aujourd’hui</button><label class="timeline-toggle-empty"><input id="timeline-hide-empty" type="checkbox" ${this.hideEmptyDays?'checked':''}/> Masquer les jours vides</label><button id="timeline-add-event" class="btn-settings">+ Événement</button><button id="timeline-add-note" class="btn-settings">+ Note libre</button><button id="timeline-add-symptoms" class="btn-settings">+ Symptômes</button></div><div class="vertical-timeline">${htmlDays || '<div class="timeline-empty-day">Aucun élément avec les filtres courants.</div>'}</div></div>`;
    screen.querySelector('#timeline-protocol-filter').onchange = async (e)=>{ this.selectedProtocolId=e.target.value; await this.render(); App.updateHeaderDate(); };
    screen.querySelector('#timeline-jump-today').onclick = async ()=>{ this.selectedDate=todayStr(); await this.render(); App.updateHeaderDate(); const el = screen.querySelector(`[data-timeline-date="${todayStr()}"]`); el?.scrollIntoView({behavior:'smooth',block:'center'}); el?.classList.add('timeline-highlight'); setTimeout(()=>el?.classList.remove('timeline-highlight'),1400); };
    screen.querySelector('#timeline-hide-empty').onchange = async (e)=>{ this.hideEmptyDays=!!e.target.checked; localStorage.setItem(this._hideEmptyStorageKey, this.hideEmptyDays?'true':'false'); await this.render(); };
    screen.querySelector('#timeline-add-event').onclick = ()=>this.openEventForm(null, protocols);
    screen.querySelector('#timeline-add-note').onclick = ()=>this.openFreeNoteForm(this.selectedDate);
    screen.querySelector('#timeline-add-symptoms').onclick = ()=>this.openSymptomsForm(this.selectedDate || todayStr());
    screen.querySelectorAll('.timeline-day-header').forEach(el=>el.onclick=()=>{ this.selectedDate=el.dataset.date; App.updateHeaderDate(); });
    screen.querySelectorAll('[data-note-edit]').forEach(b=>b.onclick=()=>this.openFreeNoteForm(b.dataset.noteEdit));
    screen.querySelectorAll('[data-ev-edit]').forEach(b=>b.onclick=()=>this.openEventForm(protocolEvents.find(e=>e.id===b.dataset.evEdit), protocols));
    screen.querySelectorAll('[data-ev-toggle]').forEach(b=>b.onclick=async()=>{ await DB.toggleProtocolEventCompleted(b.dataset.evToggle); await this.render(); await TodayScreen.render(); await JournalScreen.render(); showToast('Événement mis à jour'); });
    screen.querySelectorAll('[data-ev-del]').forEach(b=>b.onclick=async()=>{ if(!confirm('Supprimer cet événement ?')) return; await DB.deleteProtocolEvent(b.dataset.evDel); await this.render(); await TodayScreen.render(); await JournalScreen.render(); showToast('Événement supprimé'); });
  },
  async openFreeNoteForm(dateStr){ const notes = await DB.getDailyNotes(); const current = notes.find(n => n.date === dateStr); const c=`<div class="modal-header"><span class="modal-title">Note libre</span><button class="modal-close" id="modal-close-btn">✕</button></div><div class="modal-body"><input id="note-date" type="date" class="form-input" value="${escHtml(dateStr)}"/><textarea id="note-text" class="form-input" rows="4">${escHtml(current?.freeNote||'')}</textarea></div><div class="modal-footer"><button class="btn-secondary" id="note-cancel">Annuler</button><button class="btn-primary" id="note-save">Enregistrer</button></div>`; Modal.show(c); document.getElementById('modal-close-btn').onclick=()=>Modal.hide(); document.getElementById('note-cancel').onclick=()=>Modal.hide(); document.getElementById('note-save').onclick=async()=>{const chosenDate=document.getElementById('note-date').value; const content=document.getElementById('note-text').value.trim(); if(!chosenDate||!content) return showToast('Date et note obligatoires'); await DB.saveDailyNote({id:chosenDate,date:chosenDate,freeNote:content,updatedAt:new Date().toISOString(),createdAt:current?.createdAt||new Date().toISOString()}); Modal.hide(); showToast('Note enregistrée'); await this.render(); await JournalScreen.render(); if(chosenDate===todayStr()) await TodayScreen.render();}; },
  async openSymptomsForm(defaultDate){ const all = await DB.getDailySymptoms(); const current = all.find(s=>s.date===defaultDate); const row=(k,l)=>`<label>${escHtml(l)}<select id='sym-${escHtml(k)}' class='form-input'><option value='0'>0 - aucun</option><option value='1'>1 - léger</option><option value='2'>2 - modéré</option><option value='3'>3 - fort</option></select></label>`; const c=`<div class='modal-header'><span class='modal-title'>Symptômes du jour</span><button class='modal-close' id='modal-close-btn'>✕</button></div><div class='modal-body'><input id='sym-date' type='date' class='form-input' value='${escHtml(defaultDate||todayStr())}'/><div style='display:grid;grid-template-columns:1fr 1fr;gap:8px;'>${row('nausea','Nausée')}${row('fatigue','Fatigue')}${row('pain','Douleur')}${row('headache','Maux de tête')}${row('dizziness','Vertiges')}${row('mood','Humeur')}${row('sleep','Sommeil')}${row('bleeding','Saignement')}${row('other','Autre')}</div><input id='sym-other-label' class='form-input' value='${escHtml(current?.otherSymptomLabel||'')}' placeholder='Libellé autre symptôme'/></div><div class='modal-footer'><button class='btn-secondary' id='sym-cancel'>Annuler</button><button class='btn-primary' id='sym-save'>Enregistrer</button></div>`; Modal.show(c);
    const fillSymptomsForm = (date) => {
      const found = all.find((s) => s.date === date);
      const base = found?.symptoms || getDefaultSymptoms();
      Object.keys(getDefaultSymptoms()).forEach((k) => { const el = document.getElementById(`sym-${k}`); if (el) el.value = String(Number(base[k] || 0)); });
      const labelEl = document.getElementById('sym-other-label');
      if (labelEl) labelEl.value = found?.otherSymptomLabel || '';
    };
    fillSymptomsForm(defaultDate || todayStr()); document.getElementById('modal-close-btn').onclick=()=>Modal.hide(); document.getElementById('sym-cancel').onclick=()=>Modal.hide(); document.getElementById('sym-date').onchange=(e)=>fillSymptomsForm(e.target.value); document.getElementById('sym-save').onclick=async()=>{const date=document.getElementById('sym-date').value; if(!date) return showToast('Date obligatoire'); const symptoms={...getDefaultSymptoms()}; Object.keys(symptoms).forEach(k=>symptoms[k]=Math.max(0,Math.min(3,Number(document.getElementById(`sym-${k}`).value)||0))); const label=document.getElementById('sym-other-label').value.trim(); const prev=(await DB.getDailySymptoms()).find(s=>s.date===date); if(!Object.values(symptoms).some(v=>v>0)&&!label){ await DB.deleteDailySymptoms(date);} else { await DB.saveDailySymptoms({id:date,date,symptoms,otherSymptomLabel:label,createdAt:prev?.createdAt||new Date().toISOString(),updatedAt:new Date().toISOString()}); } Modal.hide(); showToast('Symptômes enregistrés'); await this.render(); await TodayScreen.render(); await JournalScreen.render();}; },
  openEventForm(ev, protocols){
    const allowedTypes = PROTOCOL_EVENT_TYPES.map((t) => t.value);
    const options = PROTOCOL_EVENT_TYPES.map(t=>`<option value="${escHtml(t.value)}" ${((ev?.type||'custom')===t.value)?'selected':''}>${escHtml(t.label)}</option>`).join('');
    const defaultProtocolId = ev?.protocolId
      || (this.selectedProtocolId !== 'all' && protocols.some((p) => p.id === this.selectedProtocolId) ? this.selectedProtocolId : '')
      || protocols.find((p) => p.status === 'active')?.id
      || protocols[0]?.id
      || '';
    const protocolOptions = protocols.map(p=>`<option value="${escHtml(p.id)}" ${(defaultProtocolId===p.id)?'selected':''}>${escHtml(p.name)}</option>`).join('');
    const c = `<div class="modal-header"><span class="modal-title">${ev?'Modifier':'Créer'} événement</span><button class="modal-close" id="modal-close-btn">✕</button></div><div class="modal-body"><div class="form-group"><label class="form-label">Protocole</label><select id="ev-protocol" class="form-input"><option value="">Sélectionner</option>${protocolOptions}</select></div><div class="form-group"><label class="form-label">Titre *</label><input id="ev-title" class="form-input" value="${escHtml(ev?.title||'')}"/></div><div class="form-row"><div class="form-group"><label class="form-label">Date *</label><input id="ev-date" type="date" class="form-input" value="${escHtml(ev?.date||this.selectedDate||todayStr())}"/></div><div class="form-group"><label class="form-label">Heure</label><input id="ev-time" type="time" class="form-input" value="${escHtml(ev?.time||'')}"/></div></div><div class="form-group"><label class="form-label">Type</label><select id="ev-type" class="form-input">${options}</select></div><div class="form-group"><label class="form-label">Notes</label><textarea id="ev-notes" class="form-input" rows="3">${escHtml(ev?.notes||'')}</textarea></div><div class="form-group"><label><input id="ev-completed" type="checkbox" ${ev?.completed?'checked':''}/> Événement terminé</label></div></div><div class="modal-footer">${ev?'<button class="btn-secondary" id="ev-delete">Supprimer</button>':''}<button class="btn-secondary" id="ev-cancel">Annuler</button><button class="btn-primary" id="ev-save">Enregistrer</button></div>`;
    Modal.show(c);
    document.getElementById('modal-close-btn').onclick=()=>Modal.hide();
    document.getElementById('ev-cancel').onclick=()=>Modal.hide();
    document.getElementById('ev-delete')?.addEventListener('click', async()=>{ if(!confirm('Supprimer cet événement ?')) return; await DB.deleteProtocolEvent(ev.id); Modal.hide(); showToast('Événement supprimé'); await this.render(); await TodayScreen.render(); await JournalScreen.render(); });
    document.getElementById('ev-save').onclick = async ()=>{
      const title = document.getElementById('ev-title').value.trim();
      const date = document.getElementById('ev-date').value;
      const protocolId = document.getElementById('ev-protocol').value;
      const time = document.getElementById('ev-time').value.trim();
      const type = document.getElementById('ev-type').value;
      if (!title) return showToast('Titre obligatoire');
      if (!date) return showToast('Date obligatoire');
      if (protocols.length && !protocolId) return showToast('Protocole obligatoire');
      if (time && !isValidTimeHHMM(time)) return showToast('Heure invalide (HH:MM)');
      if (!isValidProtocolEventType(type)) return showToast('Type invalide');
      const data = { id: ev?.id || uid(), protocolId: protocolId || null, title, date, time: time || '', type, notes: document.getElementById('ev-notes').value.trim(), completed: !!document.getElementById('ev-completed').checked, createdAt: ev?.createdAt || new Date().toISOString(), updatedAt: new Date().toISOString() };
      await DB.saveProtocolEvent(data);
      Modal.hide();
      showToast(ev ? 'Événement modifié' : 'Événement créé');
      await this.render();
      await JournalScreen.render();
      if (date === todayStr() || ev?.date === todayStr()) await TodayScreen.render();
    };
  },
  _dayHtml(dateStr, medications, phases, actionsMap, protocolEvents, protocols, notes, symptomsEntries, dosageOverrides){ const filteredMeds = this.selectedProtocolId==='all' ? medications : medications.filter((m)=>m.protocolId===this.selectedProtocolId); const filteredPhases = this.selectedProtocolId==='all' ? phases : phases.filter((p)=>p.protocolId===this.selectedProtocolId); const dayIntakes = Intakes.mergeWithActions(Intakes.generateForDate(filteredMeds, filteredPhases, dateStr, dosageOverrides), actionsMap); const dayEvents = protocolEvents.filter(e=>e.date===dateStr&&(this.selectedProtocolId==='all'||e.protocolId===this.selectedProtocolId)); const note = notes.find(n=>n.date===dateStr); const daySymptoms = symptomsEntries.find(n=>n.date===dateStr); const hasContent = dayIntakes.length || dayEvents.length || hasFreeNote(note) || hasPositiveSymptoms(daySymptoms); if (this.hideEmptyDays && !hasContent) return ''; const items=[]; for(const i of dayIntakes){items.push(`<div class='timeline-item'><div class='timeline-item-card'><div class='timeline-item-icon'>💊</div><div><div class='timeline-item-time'>${escHtml(i.displayTime||i.time||'—:—')}</div><div class='timeline-item-title'>${escHtml(i.medName||'')}</div><div class='timeline-item-detail'>${escHtml(i.dosage || '')}${i.dosageMode === 'variable' ? ' · Dosage variable' : ''}</div></div></div></div>`);} for(const e of dayEvents){items.push(`<div class='timeline-item'><div class='timeline-item-card status-event'><div class='timeline-item-icon'>📅</div><div><div class='timeline-item-time'>${escHtml(e.time||'—:—')}</div><div class='timeline-item-title'>${escHtml(e.title)} <span class='timeline-badge'>${e.completed?'Événement terminé':'Événement'}</span></div><div class='timeline-item-detail'>${escHtml(eventTypeLabelFR(e.type))}</div><div style='margin-top:6px;display:flex;gap:6px;'><button class='btn-settings' data-ev-edit='${escHtml(e.id)}'>Modifier</button><button class='btn-settings' data-ev-toggle='${escHtml(e.id)}'>${e.completed?'Réouvrir':'Terminer'}</button><button class='btn-settings' data-ev-del='${escHtml(e.id)}'>Supprimer</button></div></div></div></div>`);} if(hasFreeNote(note)) items.push(`<div class='timeline-item'><div class='timeline-item-card status-note'><div class='timeline-item-icon'>📝</div><div><div class='timeline-item-time'>Note libre</div><div class='timeline-item-detail'>${escHtml(note.freeNote)}</div><div style='margin-top:6px;'><button class='btn-settings' data-note-edit='${escHtml(dateStr)}'>Modifier</button></div></div></div></div>`); if(hasPositiveSymptoms(daySymptoms)) items.push(`<div class='timeline-item'><div class='timeline-item-card status-note'><div class='timeline-item-icon'>🌡️</div><div><div class='timeline-item-time'>Symptômes</div><div class='timeline-item-detail'>${escHtml(symptomsToText(daySymptoms,true))}</div></div></div></div>`); const label=fromDateStr(dateStr).toLocaleDateString('fr-FR',{weekday:'long',day:'numeric',month:'long'}); return `<section class='timeline-day ${dateStr===todayStr()?'is-today':''}' data-timeline-date='${escHtml(dateStr)}'><div class='timeline-rail'></div><div class='timeline-dot'></div><div class='timeline-day-content'><div class='timeline-day-header' data-date='${escHtml(dateStr)}'>${capitalize(label)}</div>${items.length?items.join(''):'<div class="timeline-empty-day">Aucune action</div>'}</div></section>`; },
};
